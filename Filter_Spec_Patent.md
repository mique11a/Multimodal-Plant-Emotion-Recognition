# 专利核心规范：基于植物电信号的闭环自适应多级数字滤波方法

本规范提取自专利文档，定义了处理植物 250Hz 低频电信号的“多级级联与闭环反馈”滤波算法。

## 1. 整体滤波架构
本发明的滤波系统摒弃了传统的静态开环滤波，构建了一种“状态感知初始化 — 动态基线评估 — 误差反馈调节”紧密耦合的闭环自适应滤波系统。整个预处理主要包含模拟滤波（如 0.1Hz 高通与 100Hz 低通）与数字滤波两部分。数字滤波核心流程如下：

## 2. 核心数字滤波算法级联
### 2.1 基础工频陷波处理
- 针对 50Hz 环境工频干扰及其高次谐波，采用中心频率固定的自适应 IIR 陷波器 。
- **自适应特性**：该陷波器的极点半径被设计为受控动态变量，以适应后级闭环联动不断变化的滤波需求 。

### 2.2 自适应 ICEEMDAN 联合小波阈值降噪
采用改进的自适应噪声完备集合经验模态分解（ICEEMDAN）联合小波阈值去噪 。

**分解阶段 (ICEEMDAN):**
- 对含噪原始信号 $x(t)$ 添加白噪声并提取第一阶 IMF 分量：
  $$IMF_1 = \frac{1}{I}\sum_{i=1}^{I}E_1(x(t) + \varepsilon_0 w^{(i)})$$ 
- 计算残差并迭代提取第 $k$ 阶分量 ($k \ge 2$)：
  $$r_{k-1}(t) = r_{k-2}(t) - IMF_{k-1}$$ (推导基于残差定义)
  $$IMF_k = \frac{1}{I}\sum_{i=1}^{I}E_k(r_{k-1}(t) + \varepsilon_{k-1} E_k(w^{(i)}))$$ 
- **停止条件**：迭代次数大于 $MaxIter$，或连续两次残差的归一化标准差 $SD$ 小于预设能量阈值 $\epsilon_{res}$：
  $$SD = \frac{\sum_{t=1}^N |r_{k-1}(t) - r_k(t)|^2}{\sum_{t=1}^N r_{k-1}^2(t)} < \epsilon_{res}$$ 

**模态筛选与小波阈值去噪:**
- 计算各分量的**排列熵 (PE)** 与**相关系数 ($\rho$)**。直接剔除 $PE > 0.6$ 且 $\rho < 0.1$ 的高频噪声无效模态分量 。
- 对保留的有效分量采用软阈值小波去噪 ：
  $$\eta_{soft}(w, \lambda_k) = sgn(w)(|w| - \lambda_k)_+$$ 
  自适应阈值为：$\lambda_k = \frac{\sigma_k \sqrt{2\ln N}}{\ln(k+1)}$ 
- 最后将去噪后的有效分量与残差叠加重构滤波信号 。

## 3. 可调初始化参数映射
算法启动时，根据环境变量将植物状态初始化，调用预设参数进行粗调 ：
| 状态 | IIR极点半径 $r(0)$ | 噪声集成层数 $I$ | 噪声系数 $\varepsilon_0(0)$ | 小波函数 | 侧重点 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **活跃态** | 0.95 | 8 | 0.20 | Sym8 | 综合保真  |
| **非活跃态**| 0.90 | 6 | 0.10 | Db4 | 防滤波过度  |
| **非正常态**| 0.99 | 10 | 0.25 | Sym12 | 深度去噪  |

## 4. 动态信号质量评估体系
在每个处理窗内，计算重构信号的质量指标 ：
1. **波形相关系数 $R$**：评估形态保真度 。
2. **信噪比增益 $GSNR$**：$GSNR = 10\lg \frac{\sum x^2(n)}{\sum (x(n) - \hat{x}(n))^2}$ 。
3. **标准化均方根误差 $NRMSE$**：$NRMSE = \frac{\sqrt{\frac{1}{N}\sum (x(n) - \hat{x}(n))^2}}{\max(x) - \min(x)}$ 。

**指数移动平均 (EMA) 动态基线：**
利用 EMA 算法持续追踪植物个体最优基线 $R_{opt}$：
$$R_{opt}(t) = \beta R_{opt}(t-1) + (1-\beta)R(t)$$
其中 $\beta$ 为遗忘因子 : 129。

## 5. 误差驱动的闭环反馈修正
构建综合滤波质量代价函数：
$$J = \omega_1 \cdot GSNR + \omega_2 \cdot R - \omega_3 \cdot NRMSE$$ 

**逆向离散参数调节：**
- **防过度滤波**：当 $R < R_{opt}$ 且 $NRMSE$ 显著超标时，减小 IIR 陷波带宽，$I \to I-1$，减小 $\varepsilon_0$，降级小波基（如 Sym12 $\to$ Sym8 $\to$ Db4） 。
- **防欠滤波**：当 $R > R_{opt}$ 且 $NRMSE$ 持续偏低时，增大 IIR 陷波带宽，$I \to I+1$，增大 $\varepsilon_0$，升级小波基（如 Sym12） 。

**连续参数梯度极值寻优：**
在小波启发式阈值中引入动态缩放系数 $\alpha(t)$ ：
$$\lambda_k(t) = \alpha(t) \cdot \frac{\sigma_k \sqrt{2\ln N}}{\ln(k+1)}$$ 
采用前向差分近似梯度上升法，在线更新 $\alpha(t)$
$$\alpha(t+1) = \alpha(t) + \mu_\alpha \cdot sgn(\alpha(t) - \alpha(t-1)) \cdot \frac{J(t) - J(t-1)}{|\alpha(t) - \alpha(t-1)| + \xi}$$
其中 $\mu_\alpha$ 为更新步长，$\xi$ 为防激荡极小常量。